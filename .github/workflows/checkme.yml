name: Check Me
on:
  push:

env:
  main: "$(/usr/bin/git log -1 --format='%H')"

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3
        with:
          repository: flooie/pingme
          ref: new-pr-ok

      - name: Run stuff for PR
        id: xyz
        run: |
          echo ${{ env.main }}
          echo "::set-output name=test::${{ env.main }}"

      - uses: actions/checkout@v3
        with:
          repository: flooie/pingme
          ref: main

      - name: Run stuff for ABC
        id: abc
        run: |
          echo ${{ env.main }}
          echo "$(/usr/bin/git log -1 --format='%H')"
          echo ${{ steps.xyz.outputs.test }}

#          curl "https://raw.githubusercontent.com/flooie/pingme/artifacts/benchmark/${{ env.main }}.csv?raw=true" --output "benchmark/${{ env.main }}.csv"
#          cd benchmark
#          ls
#          cat ${{ env.main }}.csv
